{
  "name": "webdb",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/gateway.ts",
    "build": "bun build src/gateway.ts --outdir dist --target bun",
    "start": "bun run dist/gateway.js"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "hono": "^4.6.3",
    "mime-types": "^2.1.35",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/mime-types": "^2.1.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
